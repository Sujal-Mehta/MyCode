//some important library function are
#include <stdio.h>
#include <string.h>
int main ()
{
char *ptr = "sujal bhai kese ho yar ";
int stringlength = strlen(ptr);

printf("%d" ,strlen(ptr));
printf("\n%d" ,stringlength);
return 0;
}